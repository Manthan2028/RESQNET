# ResQNet - System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        RESQNET PLATFORM                      │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  NORMAL USER     │  │   VOLUNTEER      │  │     AGENCY       │
│   (Mobile)       │  │  (Mobile/Tab)    │  │   (Desktop)      │
└────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                     │                      │
         └─────────────────────┼──────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   REACT FRONTEND    │
                    │  (TypeScript + Vite) │
                    │  Tailwind CSS        │
                    └──────────┬──────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
    ┌────▼────┐         ┌──────▼──────┐      ┌──────▼──────┐
    │ Firebase│         │  Firebase   │      │  Firebase   │
    │  Auth   │         │  Firestore  │      │   Storage   │
    └─────────┘         └─────────────┘      └─────────────┘
         │                     │                     │
         │              ┌──────▼──────┐              │
         │              │   Leaflet   │              │
         │              │  (Maps API) │              │
         │              └─────────────┘              │
         │                                           │
         └───────────────────┬───────────────────────┘
                             │
                    ┌────────▼────────┐
                    │  REAL-TIME SYNC │
                    │   & UPDATES     │
                    └─────────────────┘
```

## 🔄 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    INCIDENT LIFECYCLE                        │
└─────────────────────────────────────────────────────────────┘

1. REPORT (Normal User)
   ┌──────────────┐
   │ User Reports │
   │  Emergency   │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ GPS Captured │
   │ Photo Upload │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Firestore   │
   │   Created    │
   │ Status: GRAY │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Map Updates  │
   │  Real-time   │
   └──────────────┘

2. VERIFY (Volunteer)
   ┌──────────────┐
   │  Volunteer   │
   │   Accepts    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │   Assigned   │
   │Status:YELLOW │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Map Updates  │
   │  Real-time   │
   └──────────────┘

3. RESPOND (Volunteer)
   ┌──────────────┐
   │  On-ground   │
   │    Update    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Photo Upload │
   │Status:ORANGE │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Map Updates  │
   │  Real-time   │
   └──────────────┘

4. RESOLVE (Volunteer/Agency)
   ┌──────────────┐
   │   Marked     │
   │  Resolved    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │Status: GREEN │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Map Updates  │
   │  Real-time   │
   └──────────────┘
```

## 🗂️ Database Schema

```
FIRESTORE DATABASE
├── users/
│   └── {userId}
│       ├── uid: string
│       ├── email: string
│       ├── role: "normal" | "volunteer" | "agency"
│       ├── name: string
│       ├── phone: string
│       ├── city: string
│       ├── createdAt: timestamp
│       │
│       ├── [if volunteer]
│       │   ├── volunteerCategory: string
│       │   ├── idProofUrl: string
│       │   └── isAvailable: boolean
│       │
│       └── [if agency]
│           ├── authorityName: string
│           ├── department: string
│           └── region: string
│
└── incidents/
    └── {incidentId}
        ├── id: string
        ├── reportedBy: string
        ├── reportedByName: string
        ├── reportedByRole: string
        ├── city: string
        ├── type: string
        ├── severity: "Low" | "Medium" | "High"
        ├── location: {lat, lng}
        ├── status: "pending" | "verified" | "in-progress" | "resolved"
        ├── description: string
        ├── imageUrl?: string
        ├── timestamp: timestamp
        ├── assignedVolunteer?: string
        ├── assignedVolunteerName?: string
        └── updates: [
            {
                id: string
                timestamp: timestamp
                updatedBy: string
                updatedByRole: string
                message: string
                imageUrl?: string
                status: string
            }
        ]

FIREBASE STORAGE
├── id-proofs/
│   └── {userId}/
│       └── {filename}
├── incidents/
│   └── {timestamp}_{filename}
└── updates/
    └── {timestamp}_{filename}
```

## 🎨 Component Hierarchy

```
App.tsx
├── BrowserRouter
└── AuthProvider
    ├── Login.tsx
    ├── Registration.tsx
    │   ├── Step 1: Email/Password
    │   ├── Step 2: Role Selection
    │   └── Step 3: Profile Details
    │
    └── Protected Routes
        ├── NormalUserDashboard.tsx
        │   ├── Header
        │   ├── Report Emergency Button
        │   ├── Report Form Modal
        │   ├── LiveMap Component
        │   ├── My Incidents List
        │   └── Incident Detail Modal
        │
        ├── VolunteerDashboard.tsx
        │   ├── Header + Stats
        │   ├── LiveMap Component
        │   ├── Available Incidents
        │   ├── My Assigned Incidents
        │   ├── Update Form Modal
        │   └── Incident Detail Modal
        │
        └── AgencyDashboard.tsx
            ├── Header + Analytics
            ├── LiveMap Component
            ├── Incident Management Table
            ├── Volunteer Community List
            └── Incident Management Modal
```

## 🔐 Authentication Flow

```
┌──────────────┐
│ User Opens   │
│     App      │
└──────┬───────┘
       │
       ▼
┌──────────────┐      NO      ┌──────────────┐
│ Auth State?  │─────────────▶│   Redirect   │
│   Exists?    │               │  to Login    │
└──────┬───────┘               └──────────────┘
       │ YES
       ▼
┌──────────────┐
│ Fetch User   │
│   Profile    │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Check Role   │
└──────┬───────┘
       │
       ├─────────────┐─────────────┐
       ▼             ▼             ▼
┌──────────┐  ┌──────────┐  ┌──────────┐
│  Normal  │  │Volunteer │  │  Agency  │
│Dashboard │  │Dashboard │  │Dashboard │
└──────────┘  └──────────┘  └──────────┘
```

## 🌐 Real-Time Sync Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    REAL-TIME UPDATES                         │
└─────────────────────────────────────────────────────────────┘

User Device 1                 Firebase                User Device 2
(Normal User)                                        (Volunteer)
     │                                                    │
     │  1. Report Incident                               │
     ├──────────────────▶ Firestore ◀───────────────────┤
     │                   (onSnapshot)                    │
     │                        │                          │
     │  2. Real-time Listener │                          │
     │◀───────────────────────┤                          │
     │                        │                          │
     │                        │  3. Incident Appears     │
     │                        ├─────────────────────────▶│
     │                        │                          │
     │                        │  4. Volunteer Accepts    │
     │                        │◀─────────────────────────┤
     │                        │                          │
     │  5. Status Update      │                          │
     │◀───────────────────────┤                          │
     │                        │                          │
     │  6. Map Pin Color      │  7. Map Pin Color        │
     │     Changes (Yellow)   │     Changes (Yellow)     │
     │                        │                          │

User Device 3
(Agency)
     │
     │  8. View All Updates
     │◀───────────────────────┤
     │                        │
     │  9. Assign/Manage      │
     ├────────────────────────▶
     │                        │
     │  10. Broadcast Updates │
     │     to All Devices     │
     │                        │
```

## 📊 State Management

```
┌─────────────────────────────────────────────────────────────┐
│                    CONTEXT PROVIDERS                         │
└─────────────────────────────────────────────────────────────┘

AuthContext
├── currentUser (Firebase Auth User)
├── userProfile (Firestore User Document)
├── loading (boolean)
└── Methods:
    ├── register(data)
    ├── login(email, password)
    └── logout()

Component State (React Hooks)
├── useState
│   ├── incidents (real-time array)
│   ├── volunteers (real-time array)
│   ├── selectedIncident (modal state)
│   └── loading/error states
│
└── useEffect
    └── onSnapshot listeners
        ├── Incidents collection
        ├── Users collection
        └── Auto-cleanup on unmount
```

## 🎯 Technology Stack Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│  React Components + Tailwind CSS + TypeScript               │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    APPLICATION LAYER                         │
│  React Router + Context API + Custom Hooks                  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    INTEGRATION LAYER                         │
│  Firebase SDK + Leaflet API + Browser APIs                  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    BACKEND SERVICES                          │
│  Firebase Auth + Firestore + Storage + Hosting              │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION DEPLOYMENT                     │
└─────────────────────────────────────────────────────────────┘

Development                Build                 Production
    │                       │                        │
    ▼                       ▼                        ▼
┌─────────┐           ┌─────────┐            ┌─────────┐
│  Vite   │──────────▶│  Build  │───────────▶│ Vercel/ │
│   Dev   │           │ Process │            │Firebase │
│ Server  │           │         │            │ Hosting │
└─────────┘           └─────────┘            └─────────┘
    │                       │                        │
    │                       │                        │
    ▼                       ▼                        ▼
localhost:3000        dist/ folder            Production URL
                                                     │
                                                     ▼
                                              ┌─────────────┐
                                              │   Firebase  │
                                              │   Services  │
                                              └─────────────┘
```

---

**This architecture is designed for:**
- ✅ Real-time performance
- ✅ Scalability
- ✅ Maintainability
- ✅ Security
- ✅ User experience
